<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XamarinListViewCollectionView.Views.InicialPage">
    <ContentPage.Content>
        
        <StackLayout>

            <Button Text="ListView" FontSize="Large" Command="{Binding ListViewCommand}"/>
            <Button Text="CollectionView" FontSize="Large" Command="{Binding CollectionViewCommand}"/>
            <Button Text="SwipeCollectionView" FontSize="Large" Command="{Binding SwipeCollectionViewCommand}"/>
            <Button Text="RefreshCollectionView" FontSize="Large" Command="{Binding RefreshCollectionViewCommand}"/>
            <Button Text="SpanCollectionView" FontSize="Large" Command="{Binding SpanCollectionViewCommand}"/>
            
        </StackLayout>

    </ContentPage.Content>
</ContentPage>